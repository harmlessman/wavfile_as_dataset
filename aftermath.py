import json
import os
import set

# 여러개의 json파일 통합
def combine():
    pass

# trans폴더에서 직접 상태가 안좋은 wav파일 골라낸 후 실행하는 함수
# 실행하면 json파일에서 삭제한 파일에 해당하는 "wavfile" : text를 삭제시킴
def delete():
    pass

def 